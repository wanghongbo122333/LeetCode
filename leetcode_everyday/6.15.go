package main

import "strings"

func longestCommonPrefix(strs []string) string {
	if len(strs) == 1 {
		return strs[0]
	}
	standard:=strs[0]
	strings.Replace
	a := strs[0]
	for i := 0; i < len(s); i++ {
		if str[i] {

		}
	}
}
